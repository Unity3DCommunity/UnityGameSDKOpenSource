using UnityEngine;
using System.Collections;

namespace Core
{
    public static class CoreEnums
    {
        public enum Axis
        {
            X, Y, Z
        }

        public enum Axis2
        {
            X, Y
        }

        public enum MouseAxis
        {
            X, Y, XY
        }
        public enum GameModes
        {
            Missions,
            MultiPlayer
        }
    }

}